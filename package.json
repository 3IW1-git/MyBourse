{
  "name": "mybourse",
  "version": "1.0.0",
  "description": "Application web d'affichage de cours boursiers",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.server.json && tsc -p tsconfig.client.json",
    "build:server": "tsc -p tsconfig.server.json",
    "build:client": "tsc -p tsconfig.client.json",
    "start": "node dist/server.js",
    "dev": "tsc -p tsconfig.server.json --watch"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/express": "^4.17.21"
  }
}
